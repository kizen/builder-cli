"""Third-party code vendored verbatim (or near-verbatim) into kizen_builder.

Each subpackage carries a PROVENANCE.md recording where it came from and what,
if anything, was changed, so it can be re-synced when the upstream source moves.
"""
